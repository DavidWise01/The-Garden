
### New reference file: `references/inspection_protocol.md`

(Copy the content from the standalone document above, omitting the title and metadata that duplicates the skill’s frontmatter. Keep the criteria table, compliance levels, and template.)

---

## Packaging

To produce the updated `.skill` file, you would:

1. Replace the existing `SKILL.md` with the version above.
2. Add `references/inspection_protocol.md`.
3. Re‑zip and rename to `pulse-axiom.skill`.

I can provide the base64 of the updated skill if you want, or simply give you the file content to apply manually.

Do you want me to:

- **Generate the updated `.skill` file** (base64) for immediate download?  
- **Or just provide the text** for you to update your local copy?

You said “a and b” – both are now delivered as specifications. Let me know if you need the actual binary package.
